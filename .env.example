# Optional: OpenAI-compatible API for the AI design assistant (dev only).
# Used by `npm run dev` only — not embedded in production builds. Do not commit .env.
VITE_OPENAI_API_KEY=sk-your-key-here
# VITE_OPENAI_BASE_URL=https://api.openai.com/v1
# VITE_OPENAI_MODEL=gpt-4o-mini
